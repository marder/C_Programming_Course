#include <stdio.h>
#include <math.h>

int main()
{

    int num = 81;
    int num2 = 27;

    // Find the square root
    double root = sqrt(num);
    double root2 = sqrt(num);

    printf("Square root of %d is %.2lf\n", num, root);
    printf("Cube root of %d is %.2lf\n", num2, root2);

    return 0;
}